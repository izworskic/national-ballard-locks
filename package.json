{
  "name": "national-ballard-locks",
  "version": "1.0.0",
  "private": true,
  "description": "Ballard Locks live visitor intelligence: ships, salmon, tides, weather, water level, cameras and go-now scoring.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "verify": "npm test"
  },
  "engines": {
    "node": "22.x"
  }
}
